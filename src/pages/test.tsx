import React from 'react';

const test = () => {
  // useEffect(() => {
  //   const socket = io('http://localhost:5000', { query: { partyID: '12w3' } });
  // }, []);

  return <div>test</div>;
};

export default test;
